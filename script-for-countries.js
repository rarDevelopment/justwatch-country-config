JSON.stringify(Array.from(new Set(Array.from(document.querySelectorAll('a')).filter(c => c.attributes['href'].value.length === 3).map(d => { return JSON.stringify({ name: d.innerText.trim(), code: d.attributes['href'].value.replace('/', ''), emoji: `:flag_${d.attributes['href'].value.replace('/', '')}:`, triggerNames: [d.innerText.toLowerCase()], 'urlSearchWord': "search" }).replace(/ /g, '') }))))
